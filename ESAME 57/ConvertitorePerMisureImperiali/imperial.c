#include "imperial.h"
#include <stdlib.h>

double to_meter(const char* imperial_length) {

	const char* str = imperial_length;
	char buf[256] = { 0 };
	double lenght = 0;
	int cread = 0;
	double tmp = 0;
}